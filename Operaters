<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>JavaScript</title>
    <Script>
        var a = 12;
        var b = "MyName";
        c=13.736;

        console.log(b+a);//purpose of debugging
        console.log(c);//semicolons don't affect

        //Operators in JS
        //4+5, '+' is the operator and 4,5 are operand
        // two operators -
        //1.Unary Operator - It has just one operand (ex.  x =-x)
        //c=-c; console.log(c);

        //2.Binary Operator - two operands (x = x+2)
        c = c+a;
        console.log(c);

        //Operand - an entity which operators operate 


        var num1 = 10;
        var num2 = 12;

        //Arithmetic operations
        console.log("The value of num1 + mum2 is" + (num1 + num2));
        console.log("The value of num1 - mum2 is" + (num1 - num2));
        console.log("The value of num1 * mum2 is" + (num1 * num2));
        console.log("The value of num1 / mum2 is" + (num1 / num2));
        console.log("The value of num1 % mum2 is" + (num1 % num2));
        console.log("The value of num1 ** mum2 is" + (num1 ** num2));

        //remainder
        //4%2 = 0
        //4%3 = 1
        //2%4 = it will print small value 

        var string = "STRI'NG";
        var name = "Cat";
        var message = "This is a JS Course";
        console.log(string+name+message);
        var temp = `${name} is a good pet ${message}`;
        var len = name.length;

        console.log(temp);
        console.log(`Length of the name is \\t ${len}`);

        //one more way to crate string using construtor
        var y = new String("this");
        console.log(y);

    </Script>
</head>
<body>
    <div class="container">
        <h1>This is a paragraph</h1>
    </div>
</body>

<script>

//always use at last 
</script>
</html>
